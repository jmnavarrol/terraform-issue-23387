# BME enviro file
# Terraform config
export TFENV_TERRAFORM_VERSION='1.2.6'

# A friendly reminder to avoid surprises
bme_log "${C_BOLD}Terraform version:${C_NC}"
terraform --version
bme_log "\n${C_BOLD}Terraform workspaces:${C_NC}"
terraform workspace list
